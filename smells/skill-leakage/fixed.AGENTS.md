# AGENTS.md

## Repository Commands

```bash
pnpm install
pnpm dev
pnpm test
```

## Local Rules

- Use existing components and keep changes small.
- Prefer focused tests while developing.
- Run browser verification for user-visible UI changes.
- Use review-specific guidance only when the task is a code review.
- Use debugging-specific guidance only when reproducing or fixing a real runtime failure.

## Skill References

Do not paste full reusable skill bodies into this file. Keep skills in their own `SKILL.md` files or tool documentation. This file should only state repository-specific rules and when specialized guidance is relevant.
